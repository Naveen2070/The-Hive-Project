version: "3.8"

services:
  # ------------------------------------------------------------
  # 1. API Gateway (Nginx)
  # Acts as the single entry point.
  # Routes /api/* -> core-api and /* -> frontend
  # ------------------------------------------------------------
  gateway:
    image: nginx:alpine3.23-slim
    container_name: hive-gateway
    ports:
      - "80:80"
    volumes:
      - ./infrastructure/nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - core-api
      - frontend
    networks:
      - hive-network

  # ------------------------------------------------------------
  # 2. Frontend (React + Vite)
  # Updated for Multi-Stage Build (Nginx)
  # ------------------------------------------------------------
  frontend:
    build:
      context: ./services/frontend
      dockerfile: Dockerfile
    container_name: hive-frontend
    ports:
      - "3000:80" # Map Host 3000 -> Container 80 (Nginx default)
    restart: always
    networks:
      - hive-network

  # ------------------------------------------------------------
  # 3. Core API (Spring Boot - Kotlin)
  # ------------------------------------------------------------
  core-api:
    build:
      context: ./services/core-api
      dockerfile: Dockerfile
    container_name: hive-core-api
    ports:
      - "8080:8080"
    depends_on:
      core-db:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: prod
      # DATABASE CONFIG
      SPRING_DATASOURCE_URL: jdbc:postgresql://core-db:5432/event
      SPRING_DATASOURCE_USERNAME: ${DB_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      # SECURITY & JWT
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRATION_MS: ${JWT_EXPIRATION_MS}
      # MAIL SERVER
      MAIL_HOST: ${MAIL_HOST}
      MAIL_PORT: ${MAIL_PORT}
      MAIL_USERNAME: ${MAIL_USERNAME}
      MAIL_PASSWORD: ${MAIL_PASSWORD}
    networks:
      - hive-network

  # ------------------------------------------------------------
  # 4. Core Database (PostgreSQL)
  # ------------------------------------------------------------
  core-db:
    image: postgres:17.7-alpine
    container_name: hive-core-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: event
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME} -d event"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - hive-network

  # ------------------------------------------------------------
  # 5. Identity Service (Spring Boot)
  # ------------------------------------------------------------
  identity-service:
    build:
      context: ./services/identity-service
      dockerfile: Dockerfile
    container_name: hive-identity
    ports:
      - "8081:8081"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRATION_MS: ${JWT_EXPIRATION_MS}
      MAIL_HOST: ${MAIL_HOST}
      MAIL_PORT: ${MAIL_PORT}
      MAIL_USERNAME: ${MAIL_USERNAME}
      MAIL_PASSWORD: ${MAIL_PASSWORD}
      FRONTEND_URL: http://localhost
    depends_on:
      identity-db:
        condition: service_healthy
    networks:
      - hive-network

  # ------------------------------------------------------------
  # 6. Identity Database (PostgreSQL)
  # ------------------------------------------------------------
  identity-db:
    image: postgres:17.7-alpine
    container_name: hive-identity-db
    ports:
      - "5433:5432" # Maps to Host 5433 (Keep 5432 free for Core DB)
    environment:
      POSTGRES_DB: identity_db
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - identity_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME} -d identity_db"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - hive-network

# Define a shared network so services can talk to each other
networks:
  hive-network:
    driver: bridge

volumes:
  postgres_data:
  identity_postgres_data:
